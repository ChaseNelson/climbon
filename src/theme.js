import { createStyles } from '@material-ui/styles';

export default createStyles({
  root: {
    flexGrow: 1,
  },
  title: {
    flexGrow: 1,
    fontFamily: 'Verdana',
    fontSize: 48,
    textTransform: 'lowercase',
  },
});
